import { ConfirmModal } from '@component/modal/confirm-modal';
import { LanguageModal } from '@component/modal/language/language-modal';

const modalConfig = {
	confirm: ConfirmModal,
	language: LanguageModal,
};

export { modalConfig };
